# Užupis Cat — Retro Tribute

A playful monorepo scaffold for a retro-styled tribute site.

## Development

```bash
pnpm install
pnpm -w dev
```

Other scripts:

- `pnpm -w build`
- `pnpm -w test`
- `pnpm -w lint`
- `pnpm -w format`
